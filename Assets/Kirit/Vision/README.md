# Kirit Vision

Phase 3 currently contains only dedicated camera capture and a debug preview.
YOLO and person-presence logic are intentionally not included yet.

- `Models/` is reserved for the future YOLO ONNX model.
- `Data/` is reserved for vision configuration and test data.
